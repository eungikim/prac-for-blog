{{>layouts/header}}
<div class="container">
<table class="table">
    <thead>
    <tr>
        <th scope="col">ID</th>
        <th scope="col">Title</th>
        <th scope="col">Content</th>
    </tr>
    </thead>
    <tbody>
<!--{{#article}}-->
    <tr>
        <th scope="row">{{id}}</th>
        <td>{{title}}</td>
        <td>{{content}}</td>
    </tr>
<!--{{/article}}-->
    </tbody>
</table>

<a href="/articles/{{article.id}}/edit" class="btn btn-success">수정</a>
<a href="/articles/{{article.id}}/delete" class="btn btn-danger" id="delete">삭제</a>
<a href="/articles" class="btn btn-primary">전체 목록</a>
</div>

{{>comments/_comments}}
{{>layouts/footer}}

<script>
$('#delete').on('click', function() {
    const isDelete = confirm("삭제할까요?");
    return isDelete;
});
</script>