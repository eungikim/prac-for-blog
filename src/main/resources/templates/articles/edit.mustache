{{>layouts/header}}
<form action="/articles/update" class="container" method="post">
    <input type="hidden" value="{{article.id}}" name="id">
<!--    <div class="mb-3">-->
<!--        <label class="form-label">아아디(수정불가)</label>-->
<!--        <input type="text" class="form-control" name="id" value="{{article.id}}" readonly>-->
<!--    </div>-->
    <div class="mb-3">
        <label class="form-label">제목</label>
        <input type="text" class="form-control" name="title" value="{{article.title}}">
    </div>
    <div class="mb-3">
        <label class="form-label">내용</label>
        <textarea class="form-control" rows="3" name="content">{{article.content}}</textarea>
    </div>
    <button type="submit" class="btn btn-primary">수정</button>
    <a href="/articles/{{article.id}}" class="btn btn-success">뒤로가기</a>
</form>
{{>layouts/footer}}